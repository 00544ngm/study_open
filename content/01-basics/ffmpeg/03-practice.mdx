---
title: FFmpeg 基础练习
order: 3
difficulty: beginner
estimated_time: 25min
tags: [实践, FFmpeg, 音视频]
---

## 练习

### 目标

通过实操掌握 FFmpeg 核心命令：格式转换、视频裁剪、音频提取。

### 练习 1：视频格式转换

```bash
# 1. 准备测试视频（或用任意 mp4 文件）
# 如果没有，用 ffmpeg 生成一个测试视频：
ffmpeg -f lavfi -i testsrc=duration=10:size=640x480:rate=30 \
       -f lavfi -i sine=frequency=440:duration=10 \
       test_input.mp4

# 2. 转换为不同格式
# 转成 AVI
# 你的命令：

# 转成 GIF（缩小尺寸）
# 你的命令：

# 3. 查看转换后的文件信息
ffprobe output.avi
```

### 练习 2：视频裁剪与缩放

```bash
# 1. 裁剪视频（截取前 5 秒）
# 你的命令：

# 2. 缩放到 320x240
# 你的命令：

# 3. 从第 3 秒开始截取 4 秒并缩放到 480p
# 你的命令：

# 验证输出文件的时长和分辨率
ffprobe output.mp4 2>&1 | grep -E "Duration|Stream"
```

### 练习 3：音频处理

```bash
# 1. 从视频中提取音频（MP3 格式）
# 你的命令：

# 2. 将音频转为单声道、采样率 16000Hz
# 你的命令：

# 3. 合并视频和音频（替换原视频音轨）
# 你的命令：

# 提示：
# - 提取音频用 -vn
# - 设置采样率用 -ar
# - 设置声道数用 -ac
# - 替换音轨用 -map
```

### 验收清单

- [ ] 能用 ffmpeg 生成测试视频
- [ ] 能转换视频格式（mp4 → avi, mp4 → gif）
- [ ] 能用 `-ss` 和 `-t` 裁剪视频片段
- [ ] 能用 `-vf scale=` 缩放分辨率
- [ ] 能提取和替换音频轨道
- [ ] 能用 ffprobe 查看媒体文件信息
