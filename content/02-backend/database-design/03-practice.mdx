---
title: 数据库设计动手练习
order: 3
difficulty: advanced
estimated_time: 35min
tags: [实践, 数据库, PostgreSQL, SQL]
---

## 练习

### 目标

通过动手练习掌握 PostgreSQL 数据库设计、查询优化和迁移管理。

### 练习 1：设计 Agent 监控数据库 Schema

为一个 Agent 运行监控系统设计数据库表结构：

```sql
-- 补全表结构

-- 1. Agent 运行记录表：记录每次 Agent 执行
CREATE TABLE agent_runs (
  -- 你的代码：主键、Agent 名称、开始时间、结束时间、状态、token 消耗
);

-- 2. 工具调用日志表：记录每次工具调用
CREATE TABLE tool_calls (
  -- 你的代码：外键关联 agent_runs、工具名称、输入参数、执行时长、是否成功
);

-- 3. 错误日志表：记录运行中的异常
CREATE TABLE run_errors (
  -- 你的代码：外键关联 agent_runs、错误类型、错误消息、堆栈信息
);

-- 要求：
-- - 所有主键使用 UUID
-- - 包含 created_at TIMESTAMPTZ
-- - 添加合理的 CHECK 约束
-- - 添加必要的索引
```

### 练习 2：查询优化挑战

针对练习 1 的 Schema，优化以下查询：

```sql
-- ❌ 慢查询 1：查找特定 Agent 最近 7 天最慢的工具调用
SELECT * FROM tool_calls
WHERE agent_run_id IN (
  SELECT id FROM agent_runs WHERE agent_name = 'code-assistant'
)
ORDER BY duration_ms DESC;

-- 优化方案：添加索引或改写成 JOIN
-- 你的代码：


-- ❌ 慢查询 2：统计每日错误数量（当前不走索引）
SELECT DATE(created_at) as day, COUNT(*) as error_count
FROM run_errors
GROUP BY DATE(created_at)
ORDER BY day DESC;

-- 优化方案：
-- 你的代码：
```

### 练习 3：编写安全迁移

为练习 1 的 Schema 编写 Alembic 迁移：

```python
"""add agent monitoring tables

Revision ID: x9y8z7w6
Revises: previous_revision
Create Date: 2025-01-15
"""

from alembic import op
import sqlalchemy as sa

def upgrade():
    """
    创建 agent_runs、tool_calls、run_errors 三张表。
    要求：
    - 所有表使用 UUID 主键
    - 外键设置 ON DELETE CASCADE
    - 创建必要的索引
    - 添加 CHECK 约束
    """
    # 你的实现
    pass


def downgrade():
    """
    回滚迁移。
    注意：按依赖关系反向删除（先删依赖表，再删主表）。
    """
    # 你的实现
    pass
```

### 验收清单

- [ ] agent_runs 表包含状态约束和必要索引
- [ ] tool_calls 表通过外键关联 agent_runs
- [ ] run_errors 表通过外键关联 agent_runs
- [ ] 能识别并优化 N+1 查询
- [ ] 迁移脚本支持 upgrade 和 downgrade
- [ ] 考虑了 ON DELETE CASCADE 策略
