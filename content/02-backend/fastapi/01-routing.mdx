---
title: 路由
order: 1
difficulty: beginner
estimated_time: 15min
tags: [FastAPI, 后端]
---

## 入门

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def root():
    return {"message": "AI Agent API"}

@app.post("/chat")
def chat(prompt: str):
    return {"response": f"你说了: {prompt}"}
```

## 核心

```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel

app = FastAPI(title="AI Agent API")

# 请求模型
class ChatRequest(BaseModel):
    messages: list[dict]
    model: str = "gpt-4"
    temperature: float = 0.7

class ChatResponse(BaseModel):
    reply: str
    tokens_used: int

# Agent 聊天接口
@app.post("/api/chat", response_model=ChatResponse)
async def chat(request: ChatRequest):
    try:
        # 调用 LLM
        reply = await call_llm(request.messages, request.model)
        return ChatResponse(reply=reply, tokens_used=150)
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))

# RAG 查询接口
@app.post("/api/rag")
async def rag_search(query: str, top_k: int = 5):
    results = await search_vector_db(query, top_k)
    return {"results": results}
```

## 进阶

```python
# 动态路由 + 依赖注入
@app.get("/api/agents/{agent_id}")
async def get_agent(agent_id: str, db: Database = Depends(get_db)):
    agent = await db.fetch_one("SELECT * FROM agents WHERE id = :id", {"id": agent_id})
    if not agent:
        raise HTTPException(status_code=404, detail="Agent not found")
    return agent
```

## 练习

1. 创建一个 `/api/tools` 端点，返回可用工具列表
2. 添加请求体验证，限制 temperature 范围在 0-1 之间
