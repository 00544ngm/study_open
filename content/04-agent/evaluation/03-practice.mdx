---
title: Agent 评估练习
order: 3
difficulty: advanced
estimated_time: 35min
tags: [实践, 评估, RAGAS]
---

## 练习

### 目标

通过动手练习掌握 Agent 评估方法，包括数据集构建、指标计算和结果分析。

### 练习 1：构建评估数据集

```python
# 为 Agent 的代码生成能力构建评估集

evaluation_data = [
    {
        "task": "实现一个函数，检查字符串是否是回文",
        "expected": "def is_palindrome(s: str) -> bool:\n    return s == s[::-1]",
        "criteria": ["正确性", "效率", "类型注解"],
    },
    {
        "task": "从列表中移除重复元素",
        "expected": "def remove_duplicates(lst: list) -> list:\n    return list(set(lst))",
        "criteria": ["正确性", "简洁性"],
    },
    {
        "task": "实现二分查找",
        "expected": None,  # 可以有多个正确实现
        "criteria": ["正确性", "效率"],
    },
]


def evaluate_code_quality(generated: str, expected: str | None, criteria: list[str]) -> dict:
    """
    评估生成的代码质量。
    返回每个标准的评分（1-5）和总分。
    """
    scores = {}
    # 你的实现：对每个标准评分
    # 提示：可以检查是否包含关键函数名、是否有类型注解等
    for c in criteria:
        if c == "正确性":
            if expected:
                # 检查函数名和参数签名是否匹配
                pass
        elif c == "类型注解":
            # 检查是否有类型注解
            pass
        elif c == "效率":
            # 检查是否使用了高效方法
            pass
    return scores


# 测试数据
agent_output = "def is_palindrome(s): return s == s[::-1]"
print(evaluate_code_quality(agent_output, evaluation_data[0]["expected"], evaluation_data[0]["criteria"]))
```

### 练习 2：计算评估指标

```python
import statistics


class AgentEvaluator:
    """计算 Agent 的多维度评估指标。"""

    def __init__(self):
        self.results: list[dict] = []

    def add_result(self, task: str, success: bool, latency: float, score: float):
        """添加一次评估结果。"""
        self.results.append({
            "task": task,
            "success": success,
            "latency": latency,
            "score": score,
        })

    def success_rate(self) -> float:
        """计算成功率。"""
        # 你的实现：
        pass

    def avg_latency(self) -> float:
        """计算平均延迟。"""
        # 你的实现：
        pass

    def avg_score(self) -> float:
        """计算平均质量分数。"""
        # 你的实现：
        pass

    def report(self) -> dict:
        """生成完整评估报告。"""
        return {
            "total_tasks": len(self.results),
            "success_rate": self.success_rate(),
            "avg_latency": self.avg_latency(),
            "avg_score": self.avg_score(),
            # 添加分数分布
            "score_distribution": {},  # 你的代码：
        }


# 测试
evaluator = AgentEvaluator()
evaluator.add_result("task1", True, 1.2, 4.5)
evaluator.add_result("task2", False, 2.1, 2.0)
evaluator.add_result("task3", True, 0.8, 5.0)
print(evaluator.report())
```

### 练习 3：对比实验

```python
# 对比两个 Agent 版本的效果

class AgentVersion:
    def __init__(self, name: str):
        self.name = name
        self.results: list[bool] = []

    def run_task(self, task: str) -> bool:
        """模拟运行任务，返回是否成功。"""
        # 你的代码：模拟不同版本的成功率差异
        pass


# 设计对比实验
tasks = [
    "生成 Python 函数",
    "解释代码",
    "优化查询",
    "编写测试",
    "修复 bug",
]

# v1: 基础版本（70% 成功率）
v1 = AgentVersion("v1-basic")
# v2: 优化版本（85% 成功率）
v2 = AgentVersion("v2-optimized")

for task in tasks:
    v1.run_task(task)
    v2.run_task(task)

# 计算并对比
v1_rate = sum(v1.results) / len(v1.results)
v2_rate = sum(v2.results) / len(v2.results)

print(f"{v1.name}: {v1_rate:.0%}")
print(f"{v2.name}: {v2_rate:.0%}")
print(f"提升: {(v2_rate - v1_rate) * 100:.1f}%")
```

### 验收清单

- [ ] 能构建结构化的评估数据集
- [ ] 能设计 multi-dimension 评估标准
- [ ] 能计算成功率和平均质量分数
- [ ] 能进行 A/B 对比实验
- [ ] 能生成可量化的评估报告
