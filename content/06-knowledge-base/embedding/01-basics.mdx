---
title: Embedding 基础
order: 1
difficulty: intermediate
estimated_time: 20min
tags: [Embedding, RAG]
---

## 入门

```python
from openai import OpenAI

client = OpenAI()
response = client.embeddings.create(
    model="text-embedding-3-small",
    input="AI Agent 开发"
)
vector = response.data[0].embedding
print(f"向量维度: {len(vector)}")  # 1536
```

## 核心

```python
import numpy as np
from typing import List

def cosine_similarity(a: List[float], b: List[float]) -> float:
    a = np.array(a)
    b = np.array(b)
    return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))

# 语义搜索
documents = [
    "LangChain 是构建 Agent 的框架",
    "FastAPI 是 Python Web 框架",
    "Qdrant 是向量数据库",
]

query = "什么是 Agent 框架？"
query_vec = client.embeddings.create(
    model="text-embedding-3-small", input=query
).data[0].embedding

doc_vecs = [
    client.embeddings.create(
        model="text-embedding-3-small", input=doc
    ).data[0].embedding
    for doc in documents
]

scores = [(cosine_similarity(query_vec, dv), doc)
          for dv, doc in zip(doc_vecs, documents)]
scores.sort(reverse=True)

for score, doc in scores[:2]:
    print(f"{score:.3f}: {doc}")
```

## 练习

1. 用 text-embedding-3-small 将一份文档分块后全部向量化
2. 实现一个函数，给定查询返回最相似的 Top-3 文档块
