---
title: Docker 基础
order: 1
difficulty: beginner
estimated_time: 20min
tags: [Docker, 部署]
---

## 入门

```dockerfile
# Dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

```bash
# 构建和运行
docker build -t agent-api .
docker run -p 8000:8000 agent-api
```

## 核心

```dockerfile
# Agent 项目的 Dockerfile
FROM python:3.12-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制源码
COPY ./app ./app

# 非 root 用户
RUN useradd -m agentuser
USER agentuser

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 常用命令

```bash
docker ps                    # 运行中的容器
docker logs -f agent-api     # 查看日志
docker stop agent-api        # 停止容器
docker exec -it agent-api bash  # 进入容器
```

## 练习

1. 为 FastAPI 应用编写 Dockerfile 并成功构建镜像
2. 用 docker-compose 启动 FastAPI + PostgreSQL + Redis 三件套
